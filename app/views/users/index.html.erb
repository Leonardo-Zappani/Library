<% meta title: "Usuários", description: "Todos os usuários" %>

<div class="mt-16 mx-auto max-w-3xl px-4 sm:mt-24 text-center">
  <h1 class="text-3xl font-extrabold text-gray-800">Usuários</h1>
  <%= link_to "Novo Usuário", new_user_path,
              class: "mt-4 inline-block px-4 py-2 bg-primary-600 text-white rounded-md hover:bg-primary-700" %>
</div>

<div class="mt-12 mx-auto max-w-7xl px-6 lg:px-8 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
  <% @users.each do |user| %>
    <div class="p-2 h-full ">
      <%= render user %>
    </div>
  <% end %>
</div>
